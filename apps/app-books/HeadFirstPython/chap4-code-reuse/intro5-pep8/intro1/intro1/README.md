### Install
```
$ py -3 -m pip install pytest
$ py -3 -m pip install pytest-pep8
$ python -m pip install --upgrade pip
```

```
 The script pep8.exe is installed in 
'C:\Program Files (x86)\Microsoft Visual Studio\Shared\Python36_64\Scripts' which is not on PATH.
  Consider adding this directory to PATH or, if you prefer to suppress this warning, use --no-warn-script-location.
```

### How PEP 8–Compliant Is Our Code?

```
$ py.test --pep8 vsearch.py
```